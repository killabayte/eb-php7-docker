# Elastic Beanstalk Pilot App

* This is a Elastic Beanstalk pilot application deploying:
  * PHP7 + nginx
  * with the following PHP modules: memcached, opcache, mysql, opcache, mbstring
  * composer
